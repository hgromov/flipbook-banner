/**
 * Animation constants
 */

// Duration of page flip animation (ms)
export const ANIMATION_DURATION = 800;

// Timing function for smooth page transitions
export const ANIMATION_EASING = "cubic-bezier(0.3, 0.06, 0.2, 1)";

// Interval between autoplay page turns (ms)
export const AUTOPLAY_DURATION = 2000;
